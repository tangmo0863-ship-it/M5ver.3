# Workflow การอัปเดตข้อมูล — CIS Dashboard

เอกสารนี้สรุปว่าต้องอัปเดตอะไร เมื่อไหร่ และทำอย่างไร เพื่อไม่ให้ "ราคาหุ้น" กับ "งบการเงิน" ที่ใช้คำนวณห่างกันเกินไป

## 1. ภาพรวมรอบเวลาข้อมูล 3 ชุด

| ข้อมูล | ความถี่ที่ควรอัปเดต | อ้างอิง (โดยประมาณ) |
|---|---|---|
| **ราคาหุ้นรายวัน** (`stock_cleaned_data_2016_2025.csv`) | ทุกสัปดาห์ / ก่อนใช้งานจริงแต่ละครั้ง | ตลาดเปิดทุกวันทำการ ราคาสดที่สุด |
| **งบการเงินรายไตรมาส** (`financials_quarterly.csv`, ถ้ามี) | ทุกไตรมาส | Q1-Q3: บริษัทต้องยื่นงบภายใน ~45 วันหลังสิ้นไตรมาส (ประมาณกลาง พ.ค. / ส.ค. / พ.ย.) |
| **งบการเงินรายปี** (`master_all_8_stocks_financials.csv`) | ปีละครั้ง | งบปี (audited) ต้องยื่นภายใน ~60 วันหลังสิ้นปีบัญชี (ประมาณปลาย ก.พ. – ต้น มี.ค.) |

> วันที่ที่แน่นอนอาจต่างกันไปตามแต่ละบริษัท ควรเช็คจริงที่หน้า "งบการเงิน" ของแต่ละหุ้นบน set.or.th ก่อนดึงข้อมูล

## 2. อัปเดตราคาหุ้น (รายสัปดาห์/ตามต้องการ)

```bash
python fetch_yfinance_raw.py
```
แล้วแทนไฟล์ `Dataset/stock_cleaned_data_2016_2025.csv` เดิมด้วยผลลัพธ์ใหม่ (`auto_adjust=False` เพื่อให้ตรงกับราคา SET จริง — ดูรายละเอียดที่ตัวสคริปต์)

## 3. อัปเดตงบการเงินรายไตรมาส (ทุกไตรมาส — สำหรับ TTM)

1. ดาวน์โหลดงบไตรมาสล่าสุดของหุ้นทั้ง 8 ตัวจาก set.or.th หรือ 56-1 One Report
2. จัดเป็นไฟล์ CSV ชื่อมีคำว่า `financials_quarterly` เช่น `financials_quarterly_2026.csv` วางไว้ใน `Dataset/`
   คอลัมน์ขั้นต่ำที่ต้องมี (ชื่อใกล้เคียงกับไฟล์งบรายปีเดิมได้): `Stock, Year (ค.ศ.), Quarter (Q1-Q4), Total Revenue, NI (Parent), EPS, Total Assets, Total Equity, Total Liabilities, Current Assets, Current Liabilities, Cash Equivalent` (เพิ่ม EBIT, EBITDA, Operating CF, Free Cash Flow, Capital Expenditure ได้ถ้ามี)
3. รัน:
   ```bash
   python import_data.py
   python calculate_scores.py
   ```
   ระบบจะตรวจอัตโนมัติ: ถ้ามีข้อมูลครบ 4 ไตรมาสติดกัน → ใช้ TTM คำนวณ Fair Value/Company Health ให้เอง (label จะขึ้นเป็น `TTM (ถึง QX/YYYY)`) ถ้าข้อมูลไม่ครบ/ไม่มีไฟล์ → fallback ไปงบรายปีล่าสุดอัตโนมัติ ไม่ error

## 4. อัปเดตงบการเงินรายปี — เลื่อนหน้าต่าง 3 ปี (ปีละครั้ง)

เมื่องบปีใหม่ออก (เช่น FY2026 ยื่นราว มี.ค. 2570) ให้ทำตามนี้:

1. เพิ่มแถวข้อมูลปี 2026 ลงใน `master_all_8_stocks_financials.csv` (หรือเตรียมไฟล์ปีใหม่แยกแล้ว concat)
2. แก้ `TARGET_YEARS` ใน `import_data.py` จาก
   ```python
   TARGET_YEARS = [2023, 2024, 2025]
   ```
   เป็น
   ```python
   TARGET_YEARS = [2024, 2025, 2026]
   ```
   (เลื่อนหน้าต่างไปข้างหน้า 1 ปีเสมอ ตัดปีเก่าสุดออก เก็บ 3 ปีล่าสุดไว้เหมือนเดิม)
3. รัน `import_data.py` → `calculate_scores.py` ใหม่
4. เช็คหน้า Company Health / Fair Value ว่า label บนหัวหน้าเปลี่ยนเป็นปีใหม่ถูกต้อง

## 5. Checklist ก่อน Deploy ทุกครั้ง

- [ ] ราคาล่าสุดในไฟล์ตรงกับราคาจริงบน SET/settrade (สุ่มเช็ค 1-2 หุ้น 1 วัน)
- [ ] Sidebar แสดง "ราคาหุ้น ณ วันที่..." และ "งบการเงินที่ใช้คำนวณล่าสุด..." ตรงกับที่คาดไว้
- [ ] ถ้าใช้ TTM: ตรวจว่า 4 ไตรมาสที่ใช้ต่อเนื่องกันจริง (ไม่มีไตรมาสขาดหาย) — ถ้าขาดระบบจะ fallback เป็นงบรายปีให้เองแต่ไม่แจ้งเตือนบนหน้าจอ ควรเช็ค log ตอนรัน `calculate_scores.py`
- [ ] Commit + push ขึ้น GitHub แล้ว Reboot app บน Streamlit Cloud
